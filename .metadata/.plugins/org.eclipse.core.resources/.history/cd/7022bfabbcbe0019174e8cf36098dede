package com.br.zup.culinaria.services;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;

import com.br.zup.culinaria.models.ReceitasModel;
import com.br.zup.culinaria.repositories.ReceitaRepository;

@Service
public class ReceitaService {
	
	@Autowired
	private ReceitaRepository receitaRepository;
	
	private String salvarReceita(ReceitasModel receita) {
		
		receitaRepository.save(receita);
		
		return "Receita criada";
	}

	public Iterable<ReceitasModel> buscarTodasReceitas(){
		return receitaRepository.findAll();
	}
	
	public void deleteReceita(Integer id) {
		receitaRepository.deleteById(id);
	}
	
	public ReceitasModel buscarReceita(Integer id) {
		return receitaRepository.findById(id).get();
	}
	
	public void atualizarReceita(int id, ReceitasModel receita) {
		receita.setId(id);
		receitaRepository.save(receita);
	}
}
